# pytest.ini
[pytest]
log_cli = true
log_cli_level = INFO

# These options require pytest-asyncio to be installed in the environment.
# They control how asyncio tests are run.
# If you run into "Unknown config option" warnings, install pytest-asyncio inside your container.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
